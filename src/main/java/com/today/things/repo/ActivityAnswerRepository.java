package com.today.things.repo;

import com.today.things.model.ActivityAnswer;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ActivityAnswerRepository extends JpaRepository<ActivityAnswer, Long> {
}
